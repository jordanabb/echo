# Python packages for the ETL scripts in this directory, and for
# scripts/echo.py (which uses sqlalchemy and python-dotenv from this list).
#
#   cd packages/etl
#   python -m venv .venv
#   source .venv/bin/activate        # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
# Pinned exactly so a fresh environment matches the one these scripts were
# written and tested against. Verify with: python scripts/echo.py preflight

# Data handling
pandas==2.1.3

# Database access. psycopg2 is the driver SQLAlchemy uses for Postgres; it is
# not imported directly by our code.
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Geospatial. shapely and fiona are imported directly by the geometry loaders
# and the shapefile converter — do not rely on geopandas pulling them in.
geopandas==0.14.1
shapely==2.0.7
fiona==1.10.1

# Reads the repo-root .env that selects which database is written to.
python-dotenv==1.0.0
